import React from 'react';

const Home = () => {
    let name = "Home page";

    return (
        <div>
            <h1>{name}</h1>
            <p>This is the homepage.</p>
        </div>
    );
};

export default Home;