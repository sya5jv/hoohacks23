import React from 'react';

const TrafficPage = () => {
    let name = "Traffic page";

    return (
        <div>
            <h1>{name}</h1>
            <p>This is the homepage.</p>
        </div>
    );
};

export default TrafficPage;