import React from 'react';

const SchedulePage = () => {
    let name = "Schedule page";

    return (
        <div>
            <h1>{name}</h1>
            <p>Schedule page</p>
        </div>
    );
};

export default SchedulePage;