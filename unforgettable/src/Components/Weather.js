import React from 'react';

const WeatherPage = () => {
    let name = "Weather page";

    return (
        <div>
            <h1>{name}</h1>
            <p>This is the homepage.</p>
        </div>
    );
};

export default WeatherPage;