import React from 'react';

const SetupPage = () => {
    let name = "Setup page";

    return (
        <div>
            <h1>{name}</h1>
            <p>This is the homepage.</p>
        </div>
    );
};

export default SetupPage;